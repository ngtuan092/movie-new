const db = require('../db')
const error_msg = require('../utils')
module.exports = {
    getTrailer: (req, res, next) => {
        // trailer
        const maphim = req.params.maphim
        // query theo ma phim
    }
}